import { FabhideDirective } from './fabhide.directive';

describe('FabhideDirective', () => {
  it('should create an instance', () => {
    const directive = new FabhideDirective();
    expect(directive).toBeTruthy();
  });
});
